# Phase 2: Reservation 도메인 기능 개발

## 🎨 디자인 보존 원칙 (Mandatory)

- 모든 Mustache 파일 수정 시 **기존 HTML 구조, CSS 클래스명, 인라인 스타일을 100% 보존**한다.
- 더미 데이터(예: "A-1 데크", "₩65,000")가 있던 자리에만 Mustache 문법(`{{variable}}`)을 적용한다.
- 신규 로직 추가 시 기존 레이아웃이 깨지지 않도록 `{{#sites}}...{{/sites}}` 등의 섹션을 기존 카드 디자인 블록에 정확히 입힌다.

## 세부 태스크

### [Task 1] 예약 검색 및 가용 사이트 조회 기반 구축

- [x] **1-1. 데이터 기초 설계 (Entity & DTO)**
  - [x] `Reservation` 엔티티에 `peopleCount` 필드 추가 및 `@Builder` 반영
  - [x] `ReservationRequest`에 `SearchDTO`(검색용)와 `ReserveDTO`(예약용) 필드 구체화
  - [x] `SiteResponse.ListDTO` 정의 (사이트 정보, 구역명, 가격, 예약 가능 여부 포함)
- [x] **1-2. 데이터 조회 로직 (Repository & Service)**
  - [x] `ReservationRepository`에 기간 중복(`(existing.checkIn < request.checkOut) AND (existing.checkOut > request.checkIn)`)이며 `CONFIRMED`/`PENDING`인 예약을 제외한 사이트를 찾는 JPQL 작성
  - [x] `ReservationService.findAvailableSites` 구현 (날짜 기본값 설정, 가용 사이트 조회, 인원/구역 필터링 및 요금 계산)
- [x] **1-3. 예약 페이지 연동 (Controller & View)**
  - [x] `ReservationController.newForm`에서 검색 파라미터(`SearchDTO`) 수신 및 모델 바인딩
  - [x] `reservation/new.mustache`에 `{{#sites}}` 루프 적용 및 검색 필터 바 데이터 동기화 (기존 디자인 100% 유지)
- [x] **1-4. 단일 사이트 선택 및 예약 요약 동적 연동**
  - [x] `SiteResponse.ListDTO`에 `basePeople`, `pricePerNight` 추가 (이미 완료)
  - [x] `reservation/new.mustache`에 단일 사이트 선택/취소(토글) JS 로직 적용
  - [x] 신규 가격 공식 `(1박가+(초과인원*1만))*숙박일수` 반영 완료

### [Task 2] 예약 신청 및 결제 프로세스 구현

- [x] **2-1. 예약 생성 및 검증 (Service)**
  - [x] `ReservationService.reserve` 구현 (최종 중복 체크, 엔티티 생성 및 저장)
- [x] **2-2. 결제 페이지 및 처리 (Controller & View)**
  - [x] `ReservationController.paymentForm` 구현 (단일 사이트 수신 및 모델 바인딩)
  - [x] `reservation/payment.mustache` 디자인 유지하며 예약 정보 동적 바인딩 및 POST 전송 구현
- [x] **2-3. 예약 완료 페이지 연동 (Controller & View)**
  - [x] `ReservationController.complete` 수정 (예약 상세 정보 조회 및 모델 바인딩)
  - [x] `reservation/complete.mustache` 디자인 유지하며 예약 완료 정보 동적 바인딩

### [Task 3] 관리자 예약 현황 조회 구현

- [x] **3-1. 관리자 기능 (Admin Logic & View)**
  - [x] `AdminController.reservationList`에서 전체 예약 내역 조회 및 모델 전달
  - [x] `admin/reservation/list.mustache` 테이블 구조 유지하며 데이터 바인딩 완료
- [x] **3-2. 예약 검색 및 필터 구현**
  - [x] `AdminRequest.ReservationSearchDTO` 추가
  - [x] `ReservationRepository.findAllAdminSearch` 동적 쿼리 추가
  - [x] `admin/reservation/list.mustache` 필터 폼 연동 및 상태 유지 스크립트 적용 완료

### [Task 4] 캠핑장 배치도 인터랙티브 필터링 구현

- [x] **4-1. 배치도 구역 핀 배치 및 디자인 개선**
  - [x] 지도 이미지 위에 구역(Zone)별 핀(라벨) 배치 및 스타일 적용
  - [x] 활성/비활성 상태에 따른 핀 색상 피드백 구현
- [x] **4-2. 구역별 클라이언트 사이드 필터링 로직 구현**
  - [x] 핀 클릭 시 해당 구역 사이트 카드만 노출하는 JS 함수 작성
  - [x] 핀 재클릭(토글) 시 필터 해제 및 전체 목록 노출 로직 추가

### [Task 5] 최종 검증 및 사후 처리

- [x] **5-1. 통합 테스트 및 학습용 최종 리포트 작성**
  - [x] 전체 예약 플로우 테스트 (검색 -> 선택 -> 필터링 -> 결제 -> 관리자 확인)
  - [x] `.person/reports/`에 **Reservation 도메인 통합 리포트** 작성 (아래 내용 포함)
    - **전체 흐름(Flow)**: 검색부터 완료까지의 데이터 흐름 및 상태 변화
    - **핵심 코드 요약**: 비즈니스 로직의 정수(JPQL, Service 로직 등)와 상세 주석
    - **쉬운 비유**: 초등학생도 이해할 수 있는 도메인 로직 설명
    - **기술 및 개념 해설**: JPQL 중복 체크 쿼리, PRG 패턴, DTO 패턴 등 어려운 개념 정리
  - [x] 최종 작업 리포트 기반 자동 커밋 및 세션 종료
